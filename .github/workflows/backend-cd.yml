name: Deploy Express Backend

on:
    push:
        branches: 
            - main
        paths:
            - "express/**"
            - "pnpm-lock.yaml"
            - "pnpm-workspace.yaml"
            - "package.json"

env:
    DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
    DOCKER_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
    RENDER_BACKEND_DEPLOY_HOOK_URL: ${{ secrets.RENDER_BACKEND_DEPLOY_HOOK_URL }}

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up pnpm
              uses: pnpm/action-setup@v2
              with:
                  version: 9

            - name: Log in to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ env.DOCKER_USERNAME }}
                  password: ${{ env.DOCKER_PASSWORD }}

            - name: Install dependencies
              run: pnpm install --filter ./express...

            - name: Build Docker image
              run: |
                  docker build \
                    --platform=linux/amd64 \
                    --no-cache \
                    -f express/Dockerfile \
                    -t ${{ env.DOCKER_USERNAME }}/fintrackeasy-backend:${{ github.sha }} \
                    .

            - name: Push Docker image
              run: |
                  docker push ${{ env.DOCKER_USERNAME }}/fintrackeasy-backend:${{ github.sha }}

            - name: Trigger Render Deploy Hook
              run: |
                  curl -X POST ${{ env.RENDER_BACKEND_DEPLOY_HOOK_URL }}
